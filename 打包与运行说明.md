# KakaoChatExport 打包与运行说明

把本 SDK 打成一个**单文件 exe**，拷到「没有 Python 环境」的 Windows 电脑上双击即可导出
KakaoTalk 聊天记录为 txt / json 文件。

> 为什么必须是 exe：目标机没有 Python，纯 `.py` 脚本跑不了。PyInstaller 会把 Python
> 运行时一起打进 exe，所以产物是「一个文件、双击就运行」。

---

## 一、整体流程

```
[一台有 Python 的 Windows]  --build.bat-->  dist/KakaoChatExport.exe
                                                     |  拷贝（U盘/网盘）
                                                     v
[目标 Windows，无需任何环境]  双击 KakaoChatExport.exe  -->  聊天记录文件
```

- **打包机**：任意一台 Windows + Python 3.10+（仅打包这一次需要）。
- **目标机**：干净 Windows，已安装并登录过 KakaoTalk（否则没有聊天记录可导）。

> 我（在 macOS 上）无法替你交叉编译出 Windows exe，也无法替你下载 sqlcipher.exe。
> 下面两步需要你在 Windows 上完成。

---

## 二、准备 sqlcipher.exe（关键，只做一次）

解密 KakaoTalk 数据库依赖外部 `sqlcipher.exe`。在**打包机**上任选一种方式获取，
然后把它（及它依赖的 DLL）放进项目根的 `bin\` 目录：

- **方式 A（推荐）**：管理员 PowerShell 执行 `choco install sqlcipher`，
  到 `C:\ProgramData\chocolatey\bin\` 把 `sqlcipher.exe` 拷到本项目 `bin\`。
- **方式 B**：`scoop install sqlcipher`，从 `~\scoop\apps\sqlcipher\...\bin\` 拷出。
- **方式 C**：从 GitHub 上预编译的 SQLCipher Windows 二进制下载。

⚠ **务必带上依赖 DLL**：sqlcipher.exe 通常依赖 `libcrypto-*.dll`、`libssl-*.dll` 等。
把这些 DLL 一并放进 `bin\`，否则目标机（没环境）会报「缺少 DLL」。
验证方法：在目标机或一台干净机上直接双击 `bin\sqlcipher.exe`，不报缺 DLL 即可。

`bin\` 里的所有文件都会被 build.bat 自动打进 exe。

---

## 三、打包（在打包机上）

1. 确认已装 Python 3.10+（安装时勾选 **Add Python to PATH**）。
2. 确认 `bin\sqlcipher.exe` 已就位（见上一节）。
3. 双击 **`build.bat`**（或命令行执行 `build.bat`）。
   它会自动：装 pyinstaller/cryptography → 收集 bin/ → 打包。
4. 产物：**`dist\KakaoChatExport.exe`**（单文件，约 30–60MB）。

---

## 四、在目标机上运行

1. 把 `dist\KakaoChatExport.exe` 拷到目标 Windows 电脑（任意目录）。
2. **双击运行**。程序会中文分步引导（KakaoTalk 新版是「两步流」）：
   - 若 KakaoTalk 正在运行 → 引导你**点开几个聊天室**（让密钥进内存）→ 取密钥；
   - 引导你**完全退出 KakaoTalk** → 解密落盘的加密数据库；
   - 自动汇总并导出。
3. 每步按提示输入 `y` 回车即可（直接回车也默认继续）。
4. 导出完成后，聊天记录在 exe 同目录的 **`KakaoChat_导出_<时间戳>\`** 文件夹里。

### 导出产物
| 文件 | 内容 |
|---|---|
| `<chatId>_<聊天室名>.txt` | 每个聊天室一份，`[时间] 发送者: 内容`，人类可读 |
| `<chatId>_<聊天室名>.json` | 每个聊天室一份，结构化（authorId / type / sentAt 等字段） |
| `_summary.json` | 总览索引（聊天室数、消息数、每室文件清单） |

### 常用命令行参数（可选）
```
KakaoChatExport.exe --out D:\我的聊天记录     指定导出目录
KakaoChatExport.exe --format json            只导 json（或 txt / both）
KakaoChatExport.exe --yes                    全自动跳过确认（适合已取过密钥的日常导出）
```

---

## 五、常见问题

- **提示「未找到 sqlcipher」**：`bin\` 没放 sqlcipher.exe，或没随包打进去。重做第二、三步。
- **取密钥失败 / 0 把密钥**：
  - 先**完全退出 KakaoTalk**再重跑（新版运行时核心库是全零+独占锁，必须退出后才可读）；
  - 取钥前**点开几个聊天室**让密钥载入内存；
  - 若 KakaoTalk 以管理员运行，请右键 exe「**以管理员身份运行**」。
- **杀毒软件拦截 / 报毒**：程序用 `ReadProcessMemory` 读取 KakaoTalk 进程内存取密钥，
  属敏感操作，可能被杀软或 Windows SmartScreen 拦截。添加信任/允许即可。
  若 onefile 在临时目录执行被拦，可改用 onedir 模式（见下）。
- **目标机没装过 KakaoTalk**：没有本地聊天记录，无法导出。

### 备选：onedir 模式（杀软拦截 onefile 时）
把 `KakaoChatExport.spec` 末尾改为 onedir（去掉 EXE 里的 `a.binaries, a.datas`，
改用 `COLLECT`），产物是 `dist\KakaoChatExport\` 整个文件夹，拷文件夹到目标机，
运行里面的 `KakaoChatExport.exe`。启动更快、不易被拦。

### 备选：目标机能装 Python 时
若目标机可以装 Python 3.10+，无需打包，直接：
```
pip install cryptography
python kakao_export.py
```
（仍需 sqlcipher.exe 在 `bin\` 或 PATH 中。）
